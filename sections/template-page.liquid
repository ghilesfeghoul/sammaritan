{% render 'breadcrumb' %}

<section class="pt-7 pb-8">
  <div class="container">
    <div class="row">
      <div class="col-12">
        <!-- Heading -->
        <h3 class="mb-10 text-center">{{ page.title }}</h3>

      </div>
    </div>
    <div class="row">
      <div class="col-12">
        <div style="text-align: justify;">
          {{ page.content }}
        </div>
      </div>
    </div>
  </div>
</section>